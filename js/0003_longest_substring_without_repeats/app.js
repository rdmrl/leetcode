const subs = require('./longest_sub');

/*
const len = subs.findSubstring( 'abcabcbb' );
console.log( 'len:', len );

const len2 = subs.findSubstring( 'bbbbb' );
console.log( 'len:', len2 );
*/

const len3 = subs.findSubstring( 'pwwkew' );
console.log( 'len:', len3 );
