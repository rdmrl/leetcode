import merge from './merge';

let nums1 = [1, 2, 3, 0, 0, 0];
let nums2 = [2, 5, 6];
merge(nums1, 3, nums2, 3);

/*
let nums1: number[] = [1];
let nums2: number[] = [];
merge(nums1, 1, nums2, 0);
*/

console.log(nums1);
